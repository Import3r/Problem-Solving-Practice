#include <bits/stdc++.h>
using namespace std;

int n, number;

std::vector<int> v;

int main(){
  //freopen("mult.in" , "r" , stdin);

  std::cin >> n;

  while (n--) {

  }

  /*
    for (int i = 0; i < v.size(); i++) {
      std::cout << v[i] << ' ';
    }
    std::cout << '\n';
  */

  return 0;
}
